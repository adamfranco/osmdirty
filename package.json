{
  "name": "osmdirty",
  "version": "1.0.0",
  "description": "A tool to mark OSM tiles as dirty after edits.",
  "scripts": {
    "start": "parcel serve index.html",
    "build": "parcel build --public-url . index.html"
  },
  "dependencies": {
    "ol": "^6.15.1"
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  },
  "browserslist": "defaults"
}
